import styled from 'styled-components';

export let GroupContainer = styled.ul`
  list-style: none;
  `;

export let GroupHeader = styled.p`
  font-size: 1.5em;
  font-weight: bold;
  cursor: default;

`;
